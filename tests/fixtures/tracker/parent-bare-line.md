## Parent

#2

## What to build

A ticket whose parent reference is a bare line rather than a list item, as
`to-tickets` produced for issue #136 — the parser must not see this as an edge.

## Blocked by

None — can start immediately.
