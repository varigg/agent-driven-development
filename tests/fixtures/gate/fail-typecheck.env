# Gate fixture: the middle rung fails with a distinctive exit code; the
# rungs after it must still run (the summary reports every rung).
ADDW_RECIPE_LINT="echo lint-ran"
ADDW_RECIPE_TYPECHECK="exit 3"
ADDW_RECIPE_TESTS_AFFECTED="echo tests-ran-after-fail"
