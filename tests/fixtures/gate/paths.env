# Gate fixture: the tests recipe takes the selected paths via the {paths}
# placeholder and records what it received in $GATE_OUT (exported by the test).
ADDW_RECIPE_TESTS_AFFECTED="echo {paths} > \"\$GATE_OUT\""
