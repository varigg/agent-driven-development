# Gate fixture: every rung configured and passing. The tests recipe has no
# {paths} placeholder — it runs as-is regardless of selection.
ADDW_RECIPE_LINT="echo lint-ran"
ADDW_RECIPE_TYPECHECK="echo typecheck-ran"
ADDW_RECIPE_TESTS_AFFECTED="echo tests-ran"
